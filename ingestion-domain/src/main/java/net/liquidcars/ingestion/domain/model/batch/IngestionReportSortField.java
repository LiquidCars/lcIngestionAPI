package net.liquidcars.ingestion.domain.model.batch;

public enum IngestionReportSortField {
    id,
    processType,
    requesterParticipantId,
    inventoryId,
    externalRequestId,
    status,
    processed,
    createdAt,
    updatedAt
}
