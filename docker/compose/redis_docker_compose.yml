services:
  redis:
    image: redis:7.2-alpine
    container_name: redis-local
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - redis-net

volumes:
  redis-data:

networks:
  redis-net:
